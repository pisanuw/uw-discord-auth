[pytest]
# Put the repo root on sys.path so `import uwdiscord` works regardless of how
# pytest is invoked. Without this, the `pytest` console script (as used in CI)
# does not add the repo root to sys.path, unlike `python -m pytest`.
pythonpath = .
testpaths = tests
